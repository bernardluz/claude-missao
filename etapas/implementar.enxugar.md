Modo enxugar: apague sem pena o que o modelo alvo não usa: classe, tabela, fila, teste e configuração. O código antigo
é só consulta no histórico (`git show <sha>:<caminho>`), nunca restaurado em bloco nem mantido "por via das dúvidas".
Testes do que saiu saem junto; o que fica ganha teste do comportamento que o modelo alvo pede.
