Modo enxugar: registre a medição inicial do alvo para o antes x depois: linhas de código (sem testes), tabelas vivas
nas migrations, filas/listeners/publishers, arquivos e testes. Use comandos reproduzíveis (ex.: `git ls-files` + `wc
-l`) e diga o comando em detalhe, para o aceite medir igual no fim.
